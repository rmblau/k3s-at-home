---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jellyfin-ingress
  namespace: jellyfin
spec:
  defaultBackend:
    service:
      name: jellyfin
      port:
        name: 80
  ingressClassName: tailscale
  tls:
  - hosts:
    - jellyfin
