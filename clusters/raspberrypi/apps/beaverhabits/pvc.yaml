apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: beaverhabits-pvc
  namespace: beaverhabits
spec:
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
